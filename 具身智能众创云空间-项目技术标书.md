# 具身智能众创云空间平台

# 项目技术标书

---

**项目名称**：具身智能众创云空间（Embocloud）—— 一站式机器人生态与云控服务平台

**投标方**：（填写单位名称）

**日期**：2025 年 7 月

**版本**：V3.0（精简聚焦版：CRUD 为主 + 云控做厚 + 机器人接入）

---

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 现状评估与建设目标](#2-现状评估与建设目标)
- [3. 用户体系设计](#3-用户体系设计)
- [4. 总体技术架构](#4-总体技术架构)
- [5. 基础设施方案](#5-基础设施方案)
- [6. 后端服务架构](#6-后端服务架构)
- [7. 数据架构设计](#7-数据架构设计)
- [8. 前端工程化方案](#8-前端工程化方案)
- [9. 功能模块 CRUD 设计](#9-功能模块-crud-设计)
- [10. 云控平台专项设计（核心）](#10-云控平台专项设计核心)
- [11. 主流机器人/机器狗接入方案](#11-主流机器人机器狗接入方案)
- [12. 数据采集与反向控制](#12-数据采集与反向控制)
- [13. 安全与合规方案](#13-安全与合规方案)
- [14. DevOps 与运维方案](#14-devops-与运维方案)
- [15. 项目实施计划](#15-项目实施计划)
- [16. 质量保障体系](#16-质量保障体系)
- [17. 团队配置与预算估算](#17-团队配置与预算估算)
- [18. 风险分析与应对](#18-风险分析与应对)
- [19. 附录](#19-附录)

---

## 1. 项目概述

### 1.1 项目背景

当前具身智能与机器人产业正处于爆发期，但行业普遍存在以下痛点：

- **资源分散**：数据集、模型、仿真环境、标准认证等资源散落在各平台，开发者需在多平台间切换，效率低下。
- **门槛过高**：二次开发公司与独立开发者（OPC 一人公司）缺乏一站式工具链，从选型到部署链条过长。
- **标准缺失**：机器人产品认证涉及电磁兼容、功能安全、机械安全等多个维度，企业难以系统化掌握合规路径。
- **设备管控难**：多品牌、多型号的机器人和机器狗缺乏统一的云控平台，"数据采不到、命令发不出、状态看不见"是中小团队的普遍困境。
- **供应链分散**：机器人零部件信息获取渠道碎片化，缺乏集中的产品信息查询与供应商对接入口。

本项目定位为面向具身智能与机器人领域的**一站式生态与云控服务平台**，打通"资源检索—设备接入—数据采集—反向控制—标准认证"全链路，核心聚焦**资源 CRUD 管理 + 主流机器人/机器狗云控接入**。

### 1.2 项目定位

| 维度 | 说明 |
|------|------|
| **平台名称** | 具身智能众创云空间（Embocloud） |
| **平台类型** | 机器人生态与云控服务平台（B2B + B2D） |
| **目标用户** | 机器人二次开发公司、OPC 一人公司、高校科研团队、机器人厂商 |
| **核心价值** | CRUD 资源管理 + 主流机器人/机器狗云控接入（数据采集 + 反向控制） |
| **商业模式** | 企业订阅 + 设备接入 License + 高级功能付费 |

### 1.3 项目目标

1. **构建 12 大功能模块**：数据集中心、模型库、课程中心、云仿真平台、研发工具、云控平台、二次开发、标准认证、供应链商城（信息展示）、众创社区、论文解读、测试场地。
2. **集成 30+ 主流机器人/机器狗**：覆盖宇树全系列（Go/Go2/A1/B1/B2/H1/G1/H2）、智元 X2、傅利叶 GR-1、Figure 02、波士顿动力 Spot、ANYmal C 等。
3. **建设云控平台**：支持 MQTT/HTTP/WebSocket 多协议设备接入、实时遥测数据采集、反向控制指令下发、告警引擎、6 类数据看板。
4. **管理 1,200+ 数据集 + 24+ URDF 模型 + 86 项标准**：提供完整的 CRUD 管理后台与前端展示。
5. **建设产品信息展示平台**：不涉及在线支付，仅提供产品信息展示 + 供应商联系方式 + 在线咨询。

---

## 2. 现状评估与建设目标

### 2.1 原型现状评估

当前项目已完成**前端原型设计稿**，所有功能均为前端模拟实现：

| 维度 | 原型状态（当前） | 生产目标（本标书） |
|------|-----------------|-------------------|
| **用户系统** | 登录按钮直接设置 `isLoggedIn = true`，无注册、认证、权限 | 完整用户注册/登录（手机/邮箱）、JWT 认证、RBAC 权限、企业实名认证 |
| **数据来源** | 所有数据硬编码在 `data.js`（`window.AppData`），709 行静态 Mock | 后端 API + PostgreSQL，完整 CRUD + 分页 + 搜索 + 全文检索 |
| **购物车/订单** | 无（原型中购物车存内存，刷新即丢） | **本次不涉及**——转为产品信息展示，无在线交易 |
| **云仿真平台** | `simulation/` 下 5 个静态 HTML 页面，iframe 嵌入，无真实引擎 | 轻量仿真任务管理（对接本地 GPU 或云 GPU），任务 CRUD + 日志查看 |
| **云控平台** | 6 台假设备、5 条假告警，全静态 | **核心模块**：真实设备接入（MQTT/HTTP）、遥测数据采集（时序存储）、反向控制指令下发、告警引擎、实时看板 |
| **标准认证** | 86 项标准仅展示编号和名称 | 标准文档库 CRUD + 在线自测问卷 + 认证申请工作流 |
| **社区/论文** | 假帖、假论文，无交互 | 完整社区系统（发帖/评论/点赞）+ 论文 CMS |
| **技术架构** | Vue 3 CDN 零构建，`file://` 可用，无后端 | Vite + Vue 3 SPA + Spring Boot 后端 + PostgreSQL + Redis + InfluxDB + MinIO |
| **部署方式** | 拷贝目录，`file://` 打开 | Docker + 单机/小集群部署 + Nginx |

### 2.2 可复用资产

| 资产 | 复用方式 |
|------|---------|
| CSS 设计体系（CSS Variables、色彩规范、响应式断点） | 直接复用，迁移至 Vite 构建 |
| 13 个 Vue 组件 UI 模板 | 作为 UI 参考，Vue 3 SFC 重写 |
| 24 个 URDF 模型 + 24 张预览图 | 上传至 MinIO，直接复用 |
| 31 个数据集 + 86 项标准 + 12 个产品 + 10 门课程元数据 | 迁移至数据库作为初始数据 |
| Three.js URDF 3D 预览组件 | 保留升级，增加交互能力 |
| 仿真子系统 5 个 HTML 页面 UI | 作为云控管理后台 UI 参考 |

### 2.3 建设目标

| 阶段 | 目标 | 交付物 |
|------|------|--------|
| **一期（MVP）** | 用户系统 + 数据集/模型/标准 CRUD + **云控平台基础（设备接入+数据采集）** | 可运营基础平台 |
| **二期（增强）** | 反向控制 + 告警引擎 + 社区 + 论文 + 课程 + 场地预约 | 全功能平台 |
| **三期（优化）** | 性能优化、AI 推荐、移动端适配 | 成熟运营平台 |

---

## 3. 用户体系设计

### 3.1 用户角色定义

| 角色 | 说明 | 权限范围 |
|------|------|---------|
| **游客** | 未登录用户 | 浏览公开数据集、模型库、标准库、课程、论文；不可下载、不可使用设备管理 |
| **个人开发者** | 注册个人用户 | 全部浏览 + 下载模型/数据集 + 接入设备（最多 5 台）+ 社区发帖 + 场地预约 |
| **企业开发者** | 企业实名认证用户 | 个人全部权限 + 设备接入（无上限）+ 数据集/模型上传 + 团队管理 + 认证申请 + 反向控制 |
| **供应商** | 入驻厂商 | 企业全部权限 + 产品信息管理 |
| **管理员** | 平台运营人员 | 全部权限 + 用户管理 + 内容审核 + 系统配置 |

### 3.2 注册与认证

| 方式 | 说明 |
|------|------|
| 手机号注册 | 短信验证码 |
| 邮箱注册 | 邮箱验证链接 |
| 企业实名认证 | 提交营业执照 + 法人信息，人工审核 1-3 工作日 |

### 3.3 认证方案

- **JWT 双 Token**：Access Token（30 分钟）+ Refresh Token（7 天）
- **RBAC 权限模型**：用户 → 角色 → 权限（模块级 + 操作级 + 数据级）
- **设备级权限**：企业用户可为设备设置"只读"/"读写"权限分配给团队成员

---

## 4. 总体技术架构

### 4.1 架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│                        用户接入层                                 │
│   Web 浏览器  │  移动端 H5  │  开放 API（第三方系统集成）         │
├──────────────────────────────────────────────────────────────────┤
│                      CDN / 负载均衡层                             │
│   Nginx 反向代理 + HTTPS + 静态资源 CDN                          │
├──────────────────────────────────────────────────────────────────┤
│                       前端应用层                                  │
│   Vite + Vue 3 SPA  │  Element Plus  │  ECharts  │  Three.js    │
├──────────────────────────────────────────────────────────────────┤
│                      后端服务层（Spring Boot）                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ 用户服务 │ │ 资源服务 │ │ 社区服务 │ │ 消息服务 │           │
│  │ auth     │ │ dataset  │ │ community│ │ notify   │           │
│  │ user     │ │ model    │ │ paper    │ │          │           │
│  │ profile  │ │ standard │ │ course   │ │          │           │
│  └──────────┘ │ testfield│ └──────────┘ └──────────┘           │
│               │ devzone  │                                        │
│               │ mall     │                                        │
│               └──────────┘                                        │
│  ┌──────────────────────────────────────────────┐               │
│  │           云控核心服务（cloud-ctrl）          │               │
│  │  设备注册 │ MQTT Broker │ 数据采集管道       │               │
│  │  反向控制 │ 告警引擎    │ WebSocket 实时推送  │               │
│  └──────────────────────────────────────────────┘               │
├──────────────────────────────────────────────────────────────────┤
│                       数据存储层                                  │
│   PostgreSQL  │  Redis  │  InfluxDB  │  MinIO/COS  │  ES        │
│   (主业务库)  │ (缓存)  │ (时序)    │ (文件)      │ (搜索)     │
└──────────────────────────────────────────────────────────────────┘
```

### 4.2 设计原则

| 原则 | 说明 |
|------|------|
| **前后端分离** | 前端 SPA + 后端 RESTful API，独立部署 |
| **CRUD 标准化** | 所有资源模块遵循统一的 CRUD 接口规范，减少重复开发 |
| **云控优先** | 设备接入、数据采集、反向控制作为独立核心服务，与业务 CRUD 解耦 |
| **多协议支持** | 云控层同时支持 MQTT（设备遥测）、HTTP（管理 API）、WebSocket（实时推送） |
| **数据分层** | 关系型（业务数据）+ 缓存（热点）+ 时序（遥测）+ 对象存储（文件）+ 搜索（全文） |

### 4.3 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue 3 + Vite | 3.4+ / 5.x |
| 状态管理 | Pinia | 2.x |
| 路由 | Vue Router | 4.x |
| UI 组件库 | Element Plus | 2.x |
| 3D 渲染 | Three.js | 最新 |
| 图表 | ECharts | 5.x |
| 后端语言 | Java | 17 LTS |
| 后端框架 | Spring Boot | 3.x |
| 关系数据库 | PostgreSQL | 16 |
| 缓存 | Redis | 7.x |
| 时序数据库 | InfluxDB | 2.x |
| 对象存储 | MinIO / 腾讯云 COS | - |
| 搜索引擎 | Elasticsearch | 8.x |
| MQTT Broker | EMQX | 5.x |
| WebSocket | Spring WebSocket + STOMP | - |
| 部署 | Docker + Docker Compose / K8s | - |

---

## 5. 基础设施方案

### 5.1 服务器规划（精简版）

不再按原 V2 的 31 台集群规划，改为更务实的渐进式部署：

#### 5.1.1 生产环境（一期 MVP）

| 节点 | 规格 | 数量 | 用途 | 月费 |
|------|------|------|------|------|
| 应用服务器 | 8C16G + 200G SSD | 2 | Spring Boot 微服务 + Nginx | ¥1,200 |
| 数据库服务器 | 8C32G + 500G SSD | 1 | PostgreSQL + InfluxDB | ¥1,800 |
| 缓存/搜索服务器 | 4C16G + 200G SSD | 1 | Redis + Elasticsearch | ¥800 |
| MQTT 服务器 | 4C16G + 100G SSD | 1 | EMQX Broker | ¥600 |
| 对象存储 | 腾讯云 COS | - | URDF/图片/数据集文件，1TB | ¥50 |
| **合计** | | **5 台** | | **¥4,450/月** |

#### 5.1.2 二期扩展

| 新增 | 规格 | 用途 | 月费 |
|------|------|------|------|
| 应用服务器 | 8C16G | 扩展至 4 台，负载均衡 | +¥1,200 |
| MQTT 集群 | 4C16G × 2 | EMQX 集群高可用 | +¥1,200 |
| GPU 节点 | 8C32G + T4 | 轻量仿真任务 | +¥1,500 |
| **二期月费合计** | | | **¥8,350/月** |

### 5.2 网络架构

```
用户 → CDN → Nginx (HTTPS) → Spring Boot API (8080)
                                  ├── PostgreSQL (5432, 内网)
                                  ├── Redis (6379, 内网)
                                  ├── InfluxDB (8086, 内网)
                                  ├── Elasticsearch (9200, 内网)
                                  └── EMQX (1883/8883/8083/8084)

设备 → MQTT (8883, TLS) → EMQX → Kafka/native bridge → InfluxDB
                                           └── WebSocket → 前端
```

### 5.3 域名规划

| 域名 | 用途 |
|------|------|
| `www.embocloud.com` | Web 主站 |
| `api.embocloud.com` | REST API |
| `mqtt.embocloud.com` | MQTT 设备接入 |
| `admin.embocloud.com` | 管理后台 |

---

## 6. 后端服务架构

### 6.1 服务拆分（精简为 6 个核心服务）

| 服务 | 职责 | 主要 API | 数据库 |
|------|------|---------|--------|
| **用户服务** (user-service) | 注册、登录、JWT、个人资料、企业认证、RBAC | `/api/auth/*`, `/api/users/*` | PostgreSQL |
| **资源服务** (resource-service) | 数据集/模型/标准/课程/场地/二次开发/商城产品 CRUD | `/api/datasets/*`... | PostgreSQL + MinIO + ES |
| **社区服务** (community-service) | 帖子、评论、点赞、举报、论文 CMS | `/api/community/*`, `/api/papers/*` | PostgreSQL |
| **消息服务** (notify-service) | 站内信、邮件、短信 | `/api/notify/*` | Redis + PostgreSQL |
| **搜索服务** (search-service) | 统一搜索、索引同步 | `/api/search/*` | Elasticsearch |
| **云控服务** (cloud-ctrl-service) | 设备注册、数据采集、反向控制、告警、实时推送 | `/api/devices/*`, MQTT bridge, WebSocket | InfluxDB + PostgreSQL |

### 6.2 统一 API 规范

```
基础路径：https://api.embocloud.com/api/v1

CRUD 标准模式：
  GET    /api/v1/{resource}          — 列表（?page=1&page_size=20&keyword=&sort=）
  GET    /api/v1/{resource}/{id}     — 详情
  POST   /api/v1/{resource}          — 创建
  PUT    /api/v1/{resource}/{id}     — 更新
  DELETE /api/v1/{resource}/{id}     — 删除（软删除）

统一响应：
{
  "code": 0,
  "message": "success",
  "data": { ... },
  "pagination": { "page": 1, "page_size": 20, "total": 156 }
}

鉴权头：
  Authorization: Bearer <access_token>
```

### 6.3 错误码体系

| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 1001 | 参数校验失败 |
| 1002 | 资源不存在 |
| 2001 | 未登录 |
| 2002 | Token 过期 |
| 2003 | 无权限 |
| 3001 | 用户已存在 |
| 4001 | 设备不在线 |
| 4002 | 指令下发失败 |
| 4003 | 设备授权过期 |
| 5001 | MQTT 连接异常 |
| 9001 | 系统内部错误 |

---

## 7. 数据架构设计

### 7.1 数据库分工

| 数据库 | 用途 | 估算数据量 |
|--------|------|-----------|
| PostgreSQL | 用户、资源 CRUD、订单、设备元数据、社区帖子 | 200 GB |
| Redis | Session、缓存、限流计数器、购物车 | 8 GB |
| InfluxDB | 设备遥测时序数据（温度/速度/力矩/电流/姿态） | 500 GB |
| Elasticsearch | 数据集/标准/产品/帖子全文检索 | 100 GB |
| MinIO/COS | URDF 文件、图片、数据集文件 | 2 TB |

### 7.2 核心表设计

#### 7.2.1 用户表

```sql
CREATE TABLE users (
    id           BIGSERIAL PRIMARY KEY,
    username     VARCHAR(50) UNIQUE NOT NULL,
    email        VARCHAR(100) UNIQUE,
    phone        VARCHAR(20) UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    avatar_url   VARCHAR(500),
    status       SMALLINT DEFAULT 1,       -- 0=禁用 1=正常
    created_at   TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE user_profiles (
    user_id       BIGINT PRIMARY KEY REFERENCES users(id),
    real_name     VARCHAR(50),
    company       VARCHAR(100),
    position      VARCHAR(50),
    bio           TEXT,
    skill_tags    JSONB,
    interest_areas JSONB
);

CREATE TABLE enterprises (
    id            BIGSERIAL PRIMARY KEY,
    name          VARCHAR(100) NOT NULL,
    credit_code   VARCHAR(18) UNIQUE,
    legal_person  VARCHAR(50),
    contact_phone VARCHAR(20),
    license_url   VARCHAR(500),
    status        SMALLINT DEFAULT 0        -- 0=待审核 1=已认证 2=驳回
);
```

#### 7.2.2 资源 CRUD 通用字段模式

所有资源表（datasets / models / standards / courses / testfields / products）遵循统一字段模式：

```sql
CREATE TABLE {resource} (
    id            BIGSERIAL PRIMARY KEY,
    name          VARCHAR(200) NOT NULL,
    category      VARCHAR(50) NOT NULL,
    description   TEXT,
    tags          JSONB,                   -- ["标签1","标签2"]
    cover_image   VARCHAR(500),
    view_count    INT DEFAULT 0,
    download_count INT DEFAULT 0,
    uploader_id   BIGINT REFERENCES users(id),
    status        SMALLINT DEFAULT 1,      -- 0=下架 1=上架
    created_at    TIMESTAMPTZ DEFAULT NOW(),
    updated_at    TIMESTAMPTZ DEFAULT NOW()
);
```

#### 7.2.3 设备注册表（云控核心）

```sql
CREATE TABLE devices (
    id              BIGSERIAL PRIMARY KEY,
    device_code     VARCHAR(50) UNIQUE NOT NULL,   -- 设备唯一编码，如 DEV-001
    name            VARCHAR(100) NOT NULL,
    robot_model     VARCHAR(50) NOT NULL,           -- Go2 / H1 / Spot / ANYmal C
    robot_type      VARCHAR(50) NOT NULL,           -- 四足 / 人形 / 机械臂 / 灵巧手 / 移动
    manufacturer    VARCHAR(50),                    -- 宇树 / 波士顿动力 / Unitree
    firmware_ver    VARCHAR(20),
    owner_id        BIGINT REFERENCES users(id),
    online_status   SMALLINT DEFAULT 0,            -- 0=离线 1=在线
    last_online_at  TIMESTAMPTZ,
    registered_at   TIMESTAMPTZ DEFAULT NOW()
);
```

#### 7.2.4 反向控制指令记录

```sql
CREATE TABLE device_commands (
    id              BIGSERIAL PRIMARY KEY,
    device_id       BIGINT REFERENCES devices(id),
    command_type    VARCHAR(50) NOT NULL,   -- joint_control / gait_switch / estop / navigate
    command_body    JSONB NOT NULL,          -- 指令具体参数
    status          VARCHAR(20) DEFAULT 'pending',  -- pending/sent/success/failed/timeout
    operator_id     BIGINT REFERENCES users(id),
    sent_at         TIMESTAMPTZ,
    completed_at    TIMESTAMPTZ,
    error_message   TEXT,
    created_at      TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 8. 前端工程化方案

### 8.1 从原型到生产的迁移

| 维度 | 原型 | 生产 |
|------|------|------|
| 构建方式 | Vue 3 CDN 零构建 | Vite + Vue 3 SFC |
| 状态管理 | `reactive` + `provide/inject` | Pinia |
| 路由 | `<component :is>` 动态组件 | Vue Router 4 + 懒加载 |
| HTTP | 无（静态 `window.AppData`） | Axios + 拦截器（鉴权/错误/loading） |
| 数据 | `data.js` 硬编码 | API 调用 + Pinia 缓存 |
| 实时通信 | 无 | WebSocket（云控实时数据推送） |

### 8.2 目录结构

```
embocloud-web/
├── src/
│   ├── api/              # Axios + 各模块 API
│   ├── views/            # 页面组件（home/dataset/model/simulation/monitor/standard/mall/community/paper/testfield/devzone/course/devtool/auth）
│   ├── components/       # 公共/业务组件（UrdfViewer、DeviceCard、RealtimeChart等）
│   ├── stores/           # Pinia（user/cart/app/notify）
│   ├── router/           # 路由 + 导航守卫
│   ├── composables/      # useAuth/useWebSocket/usePagination
│   └── types/            # TypeScript 类型
├── .env.development
├── .env.production
├── vite.config.ts
└── package.json
```

### 8.3 关键能力

- **鉴权拦截器**：Axios 请求拦截自动注入 Token，响应 401 自动刷新 Token
- **WebSocket 实时通信**：设备遥测数据、告警事件实时推送到前端
- **URDF 3D 预览**：Three.js + URDFLoader + OrbitControls，支持旋转缩放、多模型对比、关节动画
- **ECharts 实时图表**：云控看板中的设备遥测折线图、设备状态饼图

---

## 9. 功能模块 CRUD 设计

以下 10 个业务模块均以 CRUD（增删改查）为核心模式，统一 API 规范，前端使用 Element Plus 表格/表单组件。

### 9.1 数据集中心

| 功能 | CRUD | 说明 |
|------|------|------|
| 数据集列表 | READ | 分页列表 + 分类筛选 + 关键词搜索 + 标签过滤 |
| 数据集详情 | READ | 名称/来源/数据量/标签/描述/官方链接/下载次数/评论 |
| 数据集创建 | CREATE | 管理员/企业用户填写表单上传 |
| 数据集编辑 | UPDATE | 修改元数据 |
| 数据集删除 | DELETE | 软删除（下架） |
| 全文检索 | READ | Elasticsearch 搜索名称/描述/标签 |
| 下载统计 | READ | Redis 计数，热门排行 |
| 收藏 | CREATE/DELETE | 用户收藏/取消收藏 |

### 9.2 模型库

| 功能 | CRUD | 说明 |
|------|------|------|
| 模型列表 | READ | 分类树（四足/人形/机械臂/灵巧手/教育/移动操作）+ 关键词搜索 |
| 模型详情 | READ | 参数 + URDF 3D 预览 + 下载 |
| 模型创建 | CREATE | 上传 URDF 文件 + 预览图 + 元数据 |
| 模型编辑 | UPDATE | 更新信息 |
| 模型删除 | DELETE | 软删除 |
| 模型对比 | READ | 选择 2-3 个模型并排对比参数 |

### 9.3 课程中心

| 功能 | CRUD | 说明 |
|------|------|------|
| 课程列表 | READ | 分类筛选 + 搜索 |
| 课程创建 | CREATE | 管理员添加课程（标题/讲师/封面/链接/描述） |
| 课程编辑/删除 | UPDATE/DELETE | 管理员操作 |
| 学习进度 | UPDATE | 用户标记"已学"/"学习中" |
| 收藏 | CREATE/DELETE | 用户收藏 |

### 9.4 标准认证中心

| 功能 | CRUD | 说明 |
|------|------|------|
| 标准列表 | READ | 13 大类筛选 + 搜索（GB/ISO/IEC 编号） |
| 标准创建/编辑/删除 | CRUD | 管理员维护标准库 |
| 认证路径 | READ | 按产品类型展示认证步骤 |
| 在线自测 | CREATE/READ | 用户提交自测问卷，系统评分生成合规度报告 |
| 认证申请 | CREATE/UPDATE | 提交认证申请 + 审核工作流 |

### 9.5 供应链商城（信息展示，不涉及支付）

**本次不涉及在线支付、购物车、订单、物流等交易功能。** 商城定位为产品信息展示平台：

| 功能 | CRUD | 说明 |
|------|------|------|
| 产品列表 | READ | 分类浏览 + 品牌筛选 + 价格区间 + 搜索 |
| 产品详情 | READ | 规格参数/图片/供应商信息 |
| 产品创建/编辑/删除 | CRUD | 供应商/管理员维护产品信息 |
| 在线咨询 | CREATE | 点击"咨询"按钮发送站内信给供应商，线下对接 |

### 9.6 众创社区

| 功能 | CRUD | 说明 |
|------|------|------|
| 帖子列表 | READ | 分类 + 热门/最新排序 + 分页 |
| 发帖 | CREATE | Markdown 富文本 + 图片上传 + 标签 |
| 帖子编辑/删除 | UPDATE/DELETE | 作者本人或管理员 |
| 评论 | CREATE | 多级评论 |
| 点赞/收藏 | CREATE/DELETE | 用户互动 |
| 举报 | CREATE | 用户举报，管理员审核 |

### 9.7 论文解读

| 功能 | CRUD | 说明 |
|------|------|------|
| 论文列表 | READ | 按会议/年份/标签筛选 |
| 论文创建 | CREATE | 管理员添加（标题/作者/会议/摘要/解读） |
| 论文编辑/删除 | UPDATE/DELETE | 管理员操作 |
| 搜索 | READ | Elasticsearch 全文搜索 |

### 9.8 测试场地

| 功能 | CRUD | 说明 |
|------|------|------|
| 场地列表 | READ | 5 个场地信息展示 |
| 场地创建/编辑/删除 | CRUD | 管理员维护 |
| 在线预约 | CREATE/UPDATE | 日历选时→提交→管理员审批→确认 |

### 9.9 二次开发

| 功能 | CRUD | 说明 |
|------|------|------|
| 机器人本体展示 | READ | 6 类机器人 + 开发套件 + 案例 |
| SDK 管理 | CRUD | 管理员上传/更新 SDK 下载链接 |
| 案例展示 | CRUD | 管理员添加/编辑案例 |

### 9.10 研发工具（智能选型）

| 功能 | CRUD | 说明 |
|------|------|------|
| 选型引导 | READ | 多维度问卷（场景/自由度/精度/预算）→ 推荐匹配型号 |
| 方案保存 | CREATE | 用户保存选型方案 |

---

## 10. 云控平台专项设计（核心）

云控平台是本项目的核心差异化模块，提供**设备注册 → 数据采集 → 实时监控 → 反向控制 → 告警管理**全链路能力。

### 10.1 整体数据流

```
┌──────────────────────┐
│  机器人/机器狗设备    │
│  (Go2/H1/Spot/...)   │
│  ┌────────────────┐  │
│  │ SDK / ROS2节点 │  │
│  │ 遥测上报 + 指令│  │
│  │ 接收执行       │  │
│  └───────┬────────┘  │
└──────────┼───────────┘
           │ MQTT (TLS) / HTTP
           ▼
┌──────────────────────┐
│   EMQX Broker 集群    │
│  设备认证 + 消息路由  │
│  主题管理 + 会话保持  │
└──────────┬───────────┘
           │
     ┌─────┴─────┐
     ▼           ▼
┌─────────┐ ┌──────────────┐
│InfluxDB │ │ cloud-ctrl   │
│时序存储 │ │ 服务         │
│         │ │ 告警引擎     │
│         │ │ 指令下发     │
└─────────┘ └──────┬───────┘
                   │
           ┌───────┴───────┐
           ▼               ▼
    ┌──────────┐    ┌──────────────┐
    │ WebSocket│    │ PostgreSQL   │
    │ 实时推送 │    │ 设备元数据   │
    │ 前端看板 │    │ 指令记录     │
    └──────────┘    │ 告警记录     │
                    └──────────────┘
```

### 10.2 设备接入

#### 10.2.1 接入方式

| 方式 | 协议 | 适用场景 | 端口 |
|------|------|---------|------|
| SDK 直连 | MQTT (TLS 1.3) | 嵌入设备端 SDK 自动上报 | 8883 |
| 网关代理 | MQTT / HTTP | 通过边缘网关汇聚多设备数据 | 8883 / 443 |
| ROS2 Bridge | MQTT | ROS2 节点通过 mqtt_bridge package 转发 | 8883 |
| HTTP REST | HTTPS | 管理类操作（设备注册、配置） | 443 |

#### 10.2.2 设备注册流程

```
用户创建设备
    │
    ├── 1. 前端填写设备信息（名称/型号/类型/厂商）
    ├── 2. 后端生成 device_code 和 MQTT 凭证（username + password）
    ├── 3. 用户下载设备端配置文件（JSON），部署到机器人上
    ├── 4. 设备首次连接 MQTT Broker → 自动注册上线
    └── 5. 前端实时显示设备在线状态
```

#### 10.2.3 MQTT 主题规范

```
设备 → 平台（遥测上报）：
  embocloud/{device_code}/telemetry        — 遥测数据（JSON）

设备 → 平台（事件上报）：
  embocloud/{device_code}/event            — 事件（上线/下线/告警/任务完成）

平台 → 设备（指令下发）：
  embocloud/{device_code}/command          — 控制指令（JSON）

平台 → 设备（配置下发）：
  embocloud/{device_code}/config           — 配置更新（JSON）

设备 → 平台（心跳）：
  embocloud/{device_code}/heartbeat        — 心跳包（每 5s）
```

### 10.3 数据采集

#### 10.3.1 遥测数据模型

每个设备上报的统一遥测 JSON 格式：

```json
{
  "device_code": "DEV-001",
  "timestamp": 1750752000000,
  "metrics": {
    "joints": {
      "joint_1": { "position": 0.52, "velocity": 0.12, "torque": 2.3, "temp": 45.2 },
      "joint_2": { "position": -0.31, "velocity": -0.05, "torque": 1.8, "temp": 42.1 }
    },
    "imu": { "accel_x": 0.01, "accel_y": -0.02, "accel_z": 9.81, "gyro_x": 0.001, "gyro_y": 0.002, "gyro_z": 0.0 },
    "battery": { "voltage": 25.2, "current": 3.5, "percentage": 78, "temp": 38.5 },
    "cpu": { "usage": 45.2, "memory": 65.1, "temp": 52.3 },
    "pose": { "x": 1.23, "y": 0.45, "z": 0.0, "roll": 0.01, "pitch": 0.02, "yaw": 1.57 }
  }
}
```

#### 10.3.2 InfluxDB 存储

```
measurement: device_telemetry
tags:        device_code, robot_model, metric_name
fields:      value (float64)
time:        设备时间戳

示例查询（某设备关节1温度最近1小时）：
SELECT value FROM device_telemetry
WHERE device_code='DEV-001' AND metric_name='joint_1_temp'
AND time > now() - 1h
```

#### 10.3.3 采集指标清单

| 指标类别 | 具体指标 | 采集频率 | 数据类型 |
|---------|---------|---------|---------|
| 关节状态 | 位置/速度/力矩/温度/电流 | 50Hz | float64 × N 关节 |
| IMU | 加速度(xyz)/角速度(xyz) | 100Hz | float64 × 6 |
| 电池 | 电压/电流/电量百分比/温度 | 1Hz | float64 × 4 |
| CPU | 使用率/内存/温度 | 1Hz | float64 × 3 |
| 姿态 | 位置(xyz)/姿态角(roll/pitch/yaw) | 20Hz | float64 × 6 |
| 足底力（四足） | 四足接触力 | 50Hz | float64 × 4 |
| 电机 | 输出力矩/转速/温度 | 50Hz | float64 × N 电机 |

### 10.4 反向控制

#### 10.4.1 指令类型

| 指令类型 | 说明 | 目标设备类型 |
|---------|------|-------------|
| 关节控制 | 设置单个/多个关节目标位置/速度/力矩 | 机械臂/人形/四足 |
| 步态切换 | 切换运动模式（行走/小跑/攀爬/待机） | 四足/人形 |
| 急停 | 紧急停止所有电机 | 全部 |
| 导航 | 设置目标航点(x,y,z,yaw) | 四足/移动机器人 |
| 末端控制 | 机械臂末端位姿控制(x,y,z,roll,pitch,yaw) | 机械臂 |
| 模式切换 | 切换自主/遥控/半自主模式 | 全部 |
| 参数配置 | 修改控制参数（PID/阻抗等） | 全部 |

#### 10.4.2 指令格式

```json
{
  "command_id": "CMD-20250713-001",
  "command_type": "joint_control",
  "device_code": "DEV-001",
  "params": {
    "joints": {
      "joint_1": { "position": 0.5, "velocity_limit": 1.0, "torque_limit": 5.0 },
      "joint_2": { "position": -0.3 }
    },
    "interpolation": "linear",
    "duration_ms": 2000
  },
  "priority": "normal",
  "timeout_ms": 5000
}
```

#### 10.4.3 安全机制

| 机制 | 说明 |
|------|------|
| 权限校验 | 仅设备 owner 和授权团队成员可下发指令 |
| 指令队列 | 同一设备指令串行执行，先进先出 |
| 急停优先 | 急停指令跳过队列，最高优先级立即执行 |
| 心跳超时保护 | 设备心跳超时 10s → 自动停止运动 → 告警 |
| 指令超时 | 指令发出后超时未完成 → 标记失败 + 告警 |
| 软限位保护 | 下发关节位置超出软限位 → 自动拒绝 |
| 操作日志 | 所有指令完整记录（谁/何时/发了什么/结果） |

#### 10.4.4 指令生命周期

```
创建 → 排队 → 下发(MQTT) → 设备执行中 → 完成/失败/超时
  │                │            │
  │                │            ├── 执行完成 → status=success
  │                │            ├── 执行失败 → status=failed + error_message
  │                │            └── 超时 → status=timeout
  │                │
  │                └── 前端实时显示指令状态（WebSocket 推送）
  │
  └── 权限校验 + 参数合法性校验
```

### 10.5 告警引擎

#### 10.5.1 告警规则

| 告警类型 | 触发条件 | 级别 | 通知方式 |
|---------|---------|------|---------|
| 关节温度超限 | 任一关节温度 > 75℃ | 警告 | 站内信 + 邮件 |
| 电量不足 | 电池 < 15% | 警告 | 站内信 + 短信 |
| 通信中断 | 心跳超时 > 60s | 严重 | 站内信 + 短信 |
| 碰撞检测 | 关节力矩突变 > 阈值 | 严重 | 站内信 + 短信 |
| 关节限位 | 位置逼近软限位 ±5% | 提示 | 站内信 |
| CPU 过载 | CPU > 90% 持续 5 分钟 | 警告 | 站内信 + 邮件 |
| 电机过热 | 电机温度 > 85℃ | 警告 | 站内信 + 邮件 |
| 电压异常 | 电池电压波动 > 20% | 严重 | 站内信 + 短信 |

#### 10.5.2 告警处理流程

```
遥测数据流入
    │
    ├── InfluxDB 存储
    └── 告警规则引擎（实时匹配）
        ├── 条件命中 → 创建告警记录
        │   ├── 去重检查（同一设备同类型 5 分钟内不重复）
        │   ├── 通知推送（站内信/邮件/短信）
        │   └── WebSocket 实时推送前端（红色闪烁）
        │
        ├── 条件恢复 → 告警自动关闭
        │   └── 记录恢复时间
        │
        └── 告警统计 → 看板展示
```

### 10.6 数据看板

| 看板 | 图表类型 | 数据源 | 刷新方式 |
|------|---------|--------|---------|
| 设备总览 | 数字卡片 + 在线/离线饼图 | PostgreSQL | 每 10s |
| 设备列表 | 可排序/筛选表格 + 状态指示灯 | PostgreSQL | 每 10s |
| 实时遥测 | 多指标折线图（关节温度/速度/力矩） | InfluxDB + WebSocket | 实时 |
| 轨迹回放 | 3D 姿态轨迹（位置+方向） | InfluxDB 历史查询 | 按时间段回放 |
| 告警历史 | 时间线 + 筛选表格 | PostgreSQL | 每 30s |
| 运行统计 | 今日运行时长柱状图 + 设备利用率饼图 | InfluxDB 聚合 | 每 5min |

---

## 11. 主流机器人/机器狗接入方案

### 11.1 接入机器人清单

#### 11.1.1 四足机器狗

| 品牌/型号 | 厂商 | SDK/API | 接入方式 | 可采集数据 | 可控制能力 |
|----------|------|---------|---------|-----------|-----------|
| **Unitree Go1** | 宇树科技 | unitree_legged_sdk (C++/Python) | ROS2 Bridge → MQTT | 关节位/速/力/IMU/电池/足底力 | 关节控制/步态切换/急停 |
| **Unitree Go2** | 宇树科技 | unitree_legged_sdk v2 | ROS2 Bridge → MQTT | 同上 + 4D LiDAR 点云 | 同上 + 导航航点 |
| **Unitree A1** | 宇树科技 | unitree_legged_sdk | ROS2 Bridge → MQTT | 关节位/速/力/IMU/电池 | 关节控制/步态切换/急停 |
| **Unitree B1** | 宇树科技 | unitree_legged_sdk | ROS2 Bridge → MQTT | 同上 + IP65 室外数据 | 同上 |
| **Unitree B2** | 宇树科技 | unitree_legged_sdk v3 | ROS2 Bridge → MQTT | 同上（升级） | 同上 + 负载控制 |
| **Boston Dynamics Spot** | Boston Dynamics | Spot SDK (Python) | Python脚本 → MQTT Bridge | 关节/IMU/相机/电池/里程计 | 关节控制/步态/导航/急停 |
| **ANYmal C** | ANYbotics | ANYmal Research SDK | ROS Bridge → MQTT | 关节力/IMU/足底力/电池 | 关节控制/步态切换/急停 |
| **云深处绝影 X20** | 云深处 | X20 SDK | 自定义 MQTT 适配器 | 同上 | 同上 |

#### 11.1.2 人形机器人

| 品牌/型号 | 厂商 | SDK/API | 接入方式 | 可采集数据 | 可控制能力 |
|----------|------|---------|---------|-----------|-----------|
| **Unitree H1** | 宇树科技 | unitree_h1_sdk | ROS2 Bridge → MQTT | 19 关节/IMU/电池/相机 | 全身关节控制/步态/急停 |
| **Unitree H1-2** | 宇树科技 | unitree_h1_sdk v2 | ROS2 Bridge → MQTT | 同上 + 灵巧手 16 DOF | 同上 + 手指控制 |
| **Unitree G1** | 宇树科技 | unitree_g1_sdk | ROS2 Bridge → MQTT | 29 关节/IMU/电池 | 全身关节/步态/灵巧手/急停 |
| **Unitree H2** | 宇树科技 | SDK（2025） | ROS2 Bridge → MQTT | 升级传感器套件 | 大负载控制 |
| **智元 X2** | 智元机器人 | AgiBot SDK | ROS2 Bridge → MQTT | 49 关节/IMU/电池 | 全身控制/灵巧手 |
| **傅利叶 GR-1** | 傅利叶智能 | Fourier SDK | ROS2 Bridge → MQTT | 54 关节/IMU/电池 | 全身控制 |
| **Figure 02** | Figure AI | 待确认（预计 API） | HTTP/MQTT 适配 | 关节/IMU/多模态感知 | 全身控制 |
| **Agility Digit** | Agility Robotics | Digit SDK | ROS Bridge → MQTT | 双足关节/IMU/电池 | 步态/导航/急停 |

#### 11.1.3 协作机械臂

| 品牌/型号 | 厂商 | SDK/API | 接入方式 | 可采集数据 | 可控制能力 |
|----------|------|---------|---------|-----------|-----------|
| **Franka Panda** | Franka Emika | libfranka (C++)/frankx (Python) | Python → MQTT Bridge | 7 关节位/速/力矩/指尖力 | 关节/笛卡尔位姿/力控/急停 |
| **UR5e** | Universal Robots | UR RTDE (Python/C++) | Python → MQTT Bridge | 6 关节位/速/力矩/末端位姿 | 关节/笛卡尔/力控/急停 |
| **FANUC M-710iC** | FANUC | FANUC Robot Interface | 网关适配（Modbus TCP → MQTT） | 关节位/速/力矩/IO | 关节/IO/急停 |
| **Unitree Z1** | 宇树科技 | unitree_z1_sdk | ROS2 Bridge → MQTT | 6 关节位/速/力矩 | 关节控制/急停 |

#### 11.1.4 灵巧手

| 品牌/型号 | 厂商 | 接入方式 | 可采集数据 | 可控制能力 |
|----------|------|---------|-----------|-----------|
| **Dexterous Hand v1** | 宇树/因时 | ROS2 Bridge → MQTT | 16 关节位/速/指尖力 | 关节位置/力控 |
| **Shadow Hand** | Shadow Robot | ROS Bridge → MQTT | 24 关节位/指尖力/触觉 | 关节控制/力控 |

### 11.2 统一设备 SDK（平台侧提供）

为降低接入门槛，平台提供**多语言设备 SDK**，封装 MQTT 连接、遥测上报、指令接收：

#### Python SDK 示例

```python
from embocloud_sdk import DeviceClient

# 初始化客户端（配置文件自动加载 MQTT 凭证）
device = DeviceClient.from_config("device_config.json")

# 注册遥测上报回调
@device.on_telemetry(interval_ms=20)  # 50Hz
def report_telemetry():
    return {
        "joints": get_joint_states(),    # 从 ROS2/驱动读取
        "imu": get_imu_data(),
        "battery": get_battery_status(),
        "cpu": get_cpu_usage(),
        "pose": get_robot_pose()
    }

# 注册指令接收回调
@device.on_command("joint_control")
def handle_joint_control(params):
    set_joint_positions(params["joints"])
    return {"status": "success"}

@device.on_command("estop")
def handle_estop(params):
    emergency_stop()
    return {"status": "stopped"}

# 启动连接
device.connect()  # 自动连接 MQTT Broker
device.start()    # 开始上报遥测 + 监听指令
```

#### C++ SDK 示例（ROS2 节点）

```cpp
#include <embocloud_sdk/device_client.hpp>

int main(int argc, char** argv) {
    rclcpp::init(argc, argv);
    auto node = std::make_shared<EmbocloudDeviceNode>("embocloud_bridge");
    
    // 自动桥接 ROS2 topic → MQTT telemetry
    node->bridge_joint_states("/joint_states");
    node->bridge_imu("/imu/data");
    node->bridge_battery("/battery_status");
    
    // 自动桥接 MQTT command → ROS2 service
    node->bridge_command("joint_control", "/set_joint_positions");
    node->bridge_command("estop", "/emergency_stop");
    
    rclcpp::spin(node);
    return 0;
}
```

### 11.3 接入步骤

```
用户端操作：
  1. 平台注册设备 → 获取 device_code + MQTT 凭证
  2. 下载 device_config.json 到机器人
  3. 安装平台 SDK（pip install embocloud-sdk）
  4. 编写设备端脚本（< 50 行代码）
  5. 启动 → 自动连接 → 平台看到设备上线

平台侧操作：
  1. 自动注册设备上线
  2. 开始接收遥测数据 → InfluxDB
  3. 前端 WebSocket 实时展示
  4. 用户可通过前端下发控制指令
```

---

## 12. 数据采集与反向控制

### 12.1 数据采集管道

```
设备端 SDK
    │
    ├── 定时采集（可配置频率：1-100Hz）
    ├── JSON 序列化
    ├── MQTT Publish (QoS 1)
    │
    ▼
EMQX Broker
    │
    ├── 设备认证（username + password）
    ├── ACL 权限校验（主题级别）
    ├── 消息持久化（磁盘队列，防止数据丢失）
    │
    ├──→ InfluxDB Writer（批量写入时序数据）
    │    └── 写入 measurement: device_telemetry
    │
    ├──→ 告警引擎（实时规则匹配）
    │    └── 命中 → 创建告警 + 推送通知
    │
    └──→ WebSocket Broadcaster
         └── 推送实时数据到前端看板
```

### 12.2 反向控制管道

```
前端 UI
    │
    ├── 用户点击"下发指令"
    ├── 选择指令类型 + 参数
    ├── 提交 → POST /api/v1/devices/{code}/command
    │
    ▼
cloud-ctrl-service
    │
    ├── 权限校验（用户是否为设备 owner）
    ├── 设备在线校验
    ├── 参数合法性校验（软限位/速度限制等）
    ├── 写入 device_commands 表（status=pending）
    │
    ├── MQTT Publish → embocloud/{device_code}/command
    │
    │   等待设备应答...
    │
    ├── 设备执行完成 → MQTT 回传结果
    │   └── 更新 device_commands (status=success/failed)
    │
    ├── 超时处理（5s 无应答 → status=timeout）
    │
    └── WebSocket 推送结果到前端
```

### 12.3 典型场景示例

#### 场景一：Unitree Go2 远程巡检

```
1. 用户在前端点击"导航至航点A"
2. 平台下发 navigate 指令：{ "x": 3.5, "y": 1.2, "z": 0, "yaw": 1.57 }
3. Go2 收到指令 → 规划路径 → 开始行走
4. 实时上报：位置(x,y,z,yaw) + 步态状态 + 相机画面
5. 到达目标点 → 上报 navigate_complete 事件
6. 前端展示"导航完成"
```

#### 场景二：Franka Panda 远程操作

```
1. 用户输入末端目标位姿 (x,y,z,roll,pitch,yaw)
2. 平台下发 joint_control 指令（逆解后的关节角度）
3. Panda 平滑移动到目标位姿
4. 实时上报：每个关节的位置/速度/力矩
5. 前端 3D 可视化实时显示机械臂姿态
6. 移动完成 → 上报 complete 事件
```

#### 场景三：批量急停

```
1. 用户在前端勾选多台设备 → 点击"批量急停"
2. 平台依次（或并行）下发 estop 指令到每台设备
3. 所有设备收到 estop → 立即停止所有电机
4. 每台设备回传 estop_confirmed
5. 前端显示每台设备急停状态，"已停止" / "超时"
```

---

## 13. 安全与合规方案

### 13.1 网络安全

| 层级 | 措施 |
|------|------|
| 传输层 | HTTPS 全站 + MQTT TLS 1.3 |
| 应用层 | JWT 鉴权 + 请求签名 + QPS 限流 |
| 服务层 | 服务间内网通信 |
| 数据层 | 数据库仅内网访问、密码 bcrypt 加密、敏感字段 AES-256 |

### 13.2 设备安全

| 维度 | 措施 |
|------|------|
| 设备认证 | MQTT 用户名 + 密码 + TLS 双向认证（可选） |
| 指令鉴权 | 仅设备 owner 及授权成员可下发指令 |
| 急停超控 | 急停指令绕过所有权限限制，任何已登录用户均可触发 |
| 固件安全 | 设备固件版本校验，低版本告警提示升级 |

### 13.3 合规要求

| 法规 | 应对措施 |
|------|---------|
| 《网络安全法》 | 网站备案、日志留存 6 个月 |
| 《数据安全法》 | 设备数据分类分级、用户授权管理 |
| 《个人信息保护法》 | 隐私政策 + 用户协议 + 最小化收集 |

---

## 14. DevOps 与运维方案

### 14.1 部署方案

一期采用 **Docker Compose** 单机/双机部署（简单可靠），二期可平滑迁移 K8s：

```yaml
# docker-compose.yml 核心服务
services:
  nginx:       # 反向代理 + HTTPS
  postgres:    # PostgreSQL 16
  redis:       # Redis 7
  influxdb:    # InfluxDB 2
  elasticsearch: # ES 8
  minio:       # MinIO 对象存储
  emqx:        # EMQX MQTT Broker
  api:         # Spring Boot API (×2)
  web:         # Nginx 前端静态文件
```

### 14.2 CI/CD

```
开发 → Git Push → GitLab CI
  ├── 代码检查（ESLint / Checkstyle）
  ├── 单元测试（Jest / JUnit）
  ├── Docker 镜像构建
  └── 部署到测试环境
       └── 手动触发 → 部署到生产环境
```

### 14.3 监控告警

| 监控对象 | 工具 |
|---------|------|
| 服务器资源 | Prometheus + node_exporter + Grafana |
| 应用指标 | Spring Boot Actuator |
| MQTT Broker | EMQX Dashboard + Prometheus |
| 日志 | Filebeat → Elasticsearch → Kibana |
| 业务指标 | 自定义指标（在线设备数、指令成功率） |

---

## 15. 项目实施计划

### 15.1 总体进度（20 周）

| 阶段 | 周期 | 目标 |
|------|------|------|
| **第一阶段：基础架构 + 用户** | W1-W3 | 基础设施 + 用户系统 + 前端工程化 |
| **第二阶段：核心 CRUD** | W4-W8 | 数据集/模型/标准/课程/场地/商城 CRUD |
| **第三阶段：云控平台（核心）** | W9-W14 | 设备接入 + 数据采集 + 反向控制 + 告警 + 看板 |
| **第四阶段：生态模块** | W15-W17 | 社区/论文/二次开发/选型 |
| **第五阶段：联调测试上线** | W18-W20 | 集成测试 + 性能优化 + 上线 |

### 15.2 详细任务

#### 第一阶段：基础架构 + 用户系统（W1-W3）

| 周 | 任务 |
|----|------|
| W1 | 云服务器采购 + PostgreSQL/Redis/InfluxDB/MinIO/EMQX 部署 + Docker Compose |
| W1 | Vite + Vue 3 前端项目搭建 + 路由/状态管理/Element Plus |
| W2 | 用户服务：注册/登录/JWT/RBAC 权限 |
| W2 | 前端：登录/注册页面 + 鉴权拦截器 + 导航框架 |
| W3 | 企业实名认证 + 个人中心页面 |

#### 第二阶段：核心 CRUD（W4-W8）

| 周 | 任务 |
|----|------|
| W4 | 数据集服务：CRUD + 分类 + Elasticsearch 搜索 |
| W4 | 前端：数据集列表/详情/搜索/筛选 |
| W5 | 模型服务：CRUD + 分类树 + URDF 3D 预览升级 |
| W5 | 前端：模型库列表/详情/3D 预览 |
| W6 | 标准服务：CRUD + 认证路径 + 自测问卷引擎 |
| W6 | 前端：标准库/自测/认证申请 |
| W7 | 课程/场地/商城产品 CRUD + 前端 |
| W8 | 搜索服务（ES 索引同步）+ 消息服务（站内信/邮件） |

#### 第三阶段：云控平台（W9-W14）★核心

| 周 | 任务 |
|----|------|
| W9 | EMQX Broker 配置 + 设备注册 API + MQTT 主题设计 |
| W10 | 设备 SDK（Python + C++/ROS2）开发 |
| W11 | 数据采集管道（MQTT → InfluxDB）+ 遥测存储 |
| W12 | WebSocket 实时推送 + 前端实时遥测看板（ECharts 折线图） |
| W13 | 反向控制：指令 API + 指令队列 + MQTT 下发 + 安全机制 |
| W14 | 告警引擎（规则匹配）+ 历史回放 + 设备总览看板 |
| | **接入验证**：完成 Unitree Go2 + Franka Panda 实机接入验证 |

#### 第四阶段：生态模块（W15-W17）

| 周 | 任务 |
|----|------|
| W15 | 社区服务：帖子/评论/点赞/举报 + 前端 |
| W15 | 论文服务：CMS + 搜索 + 前端 |
| W16 | 二次开发 + 研发工具（选型引擎）+ 前端 |
| W17 | 移动端适配 + 管理后台完善 |

#### 第五阶段：联调测试上线（W18-W20）

| 周 | 任务 |
|----|------|
| W18 | 集成测试 + E2E 测试（Playwright） |
| W19 | 性能测试（k6）+ 安全审计 |
| W20 | 生产环境部署 + 域名/SSL/CDN 配置 → **正式上线** |

### 15.3 里程碑

```
W3  ──── M1: 基础平台就绪（可登录、可鉴权）
W8  ──── M2: 全部 CRUD 模块完成
W14 ──── M3: 云控平台完成（设备接入+数据采集+反向控制+告警）★
W17 ──── M4: 全功能完成
W20 ──── M5: 正式上线
```

---

## 16. 质量保障体系

### 16.1 代码质量

| 维度 | 标准 |
|------|------|
| 前端规范 | ESLint + Prettier |
| 后端规范 | Checkstyle + SonarQube |
| 代码审查 | PR 至少 1 人 Review |
| 测试覆盖 | 核心逻辑 ≥80% |
| API 文档 | Swagger/OpenAPI 3.0 |

### 16.2 验收标准

| 验收项 | 标准 |
|--------|------|
| 首屏加载 | ≤2s |
| API 响应 | P95 ≤200ms（CRUD 列表查询） |
| 并发能力 | 500 并发用户 |
| 遥测延迟 | 设备上报 → 前端显示 ≤1s |
| 指令下发延迟 | 前端点击 → MQTT 发出 ≤200ms |
| 可用性 | SLA 99.5% |
| 移动端适配 | 375px 宽度布局正常 |
| 浏览器兼容 | Chrome 90+ / Edge 90+ / Safari 14+ / Firefox 90+ |

---

## 17. 团队配置与预算估算

### 17.1 团队结构（10 人）

| 角色 | 人数 | 周期 |
|------|------|------|
| 项目经理 | 1 | 全周期 |
| 全栈开发（偏后端） | 3 | 全周期 |
| 前端开发 | 2 | 全周期 |
| 云控/IoT 开发 | 1 | W9-W17 |
| DevOps | 1 | W1-W5 + W18-W20 |
| UI 设计师 | 1 | W1-W8 |
| 测试工程师 | 1 | W14-W20 |
| **合计** | **10 人** | |

### 17.2 人力成本

| 角色 | 月薪（万元） | 人月数 | 小计（万元） |
|------|-----------|--------|------------|
| 项目经理 | 2.5 | 5 | 12.5 |
| 全栈开发 | 2.5 × 3 | 15 | 37.5 |
| 前端开发 | 2.0 × 2 | 10 | 20 |
| 云控/IoT 开发 | 3.0 | 2.5 | 7.5 |
| DevOps | 2.5 | 1.5 | 3.75 |
| UI 设计师 | 1.8 | 2 | 3.6 |
| 测试工程师 | 1.5 | 1.5 | 2.25 |
| **合计** | | **37.5 人月** | **¥87.1 万** |

### 17.3 基础设施成本

| 项目 | 月费 | 5 个月总计 |
|------|------|-----------|
| 生产服务器（5 台） | ¥4,450 | ¥22,250 |
| 二期扩展（W14+） | +¥3,900 | ¥7,800 |
| 对象存储（2TB） | ¥100 | ¥500 |
| 短信/邮件 | ¥500 | ¥2,500 |
| 域名/SSL | ¥200 | ¥1,000 |
| **合计** | | **约 ¥3.4 万** |

### 17.4 总预算

| 项目 | 金额 |
|------|------|
| 人力成本 | ¥87.1 万 |
| 基础设施 | ¥3.4 万 |
| 不可预见费用（10%） | ¥9.1 万 |
| **项目总计** | **约 ¥99.6 万** |

---

## 18. 风险分析与应对

| 风险 | 等级 | 应对措施 |
|------|------|---------|
| 不同品牌机器人 SDK 差异大，接入适配工作量大 | 高 | 优先接入主流品牌（宇树全系列、Spot、Franka）；提供 Python/ROS2 标准 SDK 降低适配成本；预留 W10-W11 两周缓冲 |
| MQTT Broker 高并发下性能瓶颈 | 中 | EMQX 单节点可支撑百万连接，一期设备量远低于此；预留集群扩展方案 |
| 设备通信不稳定（野外/地下室网络差） | 中 | SDK 内置本地缓存 + 断网重连 + 数据补传；心跳超时自动告警 |
| 人员变动 | 中 | 核心模块文档化 + 代码审查 + 知识共享 |

---

## 19. 附录

### 19.1 原型可复用资产清单

| 资产 | 数量 | 迁移目标 |
|------|------|---------|
| CSS 设计体系 | 1 套 | Vite 项目 SCSS Variables |
| Vue 组件模板 | 13 个 | Vue 3 SFC 重写 |
| URDF 模型文件 | 18 个 | MinIO 对象存储 |
| 模型预览图 | 24 个 | MinIO |
| 数据集元数据 | 31 条 | PostgreSQL 初始数据 |
| 标准数据 | 86 条 | PostgreSQL 初始数据 |
| 产品数据 | 12 条 | PostgreSQL 初始数据 |
| 课程数据 | 10 条 | PostgreSQL 初始数据 |
| 测试场地数据 | 5 个 | PostgreSQL 初始数据 |
| 图片资源 | 28 个 | MinIO + CDN |
| 仿真页面 UI | 5 个 | 云控页面参考 |

### 19.2 第三方服务

| 服务 | 用途 | 费用 |
|------|------|------|
| 腾讯云 CVM | 服务器 | ¥4,450/月 |
| 腾讯云 COS | 对象存储 | ¥100/月 |
| 阿里云短信 | 验证码/告警 | ¥0.045/条 |
| 阿里云邮件推送 | 通知邮件 | ¥0.0005/封 |

### 19.3 术语表

| 术语 | 说明 |
|------|------|
| Embodied AI | 具身智能 |
| URDF | 统一机器人描述格式 |
| MQTT | 轻量级消息传输协议（IoT 标准协议） |
| EMQX | 开源 MQTT Broker |
| ROS2 | 机器人操作系统第二代 |
| Telemetry | 遥测数据 |
| Reverse Control | 反向控制（平台→设备指令下发） |
| CRUD | Create/Read/Update/Delete |
| RBAC | 基于角色的访问控制 |
| JWT | JSON Web Token |

---

**—— 文档结束 ——**

**投标方**：（填写单位名称）

**授权代表**：（签字）

**日期**：2025 年 7 月 日
